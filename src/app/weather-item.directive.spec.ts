import { WeatherItemDirective } from './weather-item.directive';

describe('WeatherItemDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherItemDirective();
    expect(directive).toBeTruthy();
  });
});
